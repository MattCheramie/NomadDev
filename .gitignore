# Go
bin/
*.exe
*.test
*.out
coverage.txt

# Env
.env
.env.local

# IDE
.vscode/
.idea/
*.swp

# Mobile (Phase 5)
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/ios/Pods/
mobile/android/.gradle/
mobile/android/app/build/

# Built SPA bundle — regenerated by `make build-mobile`. Only the stub
# index.html (and .gitkeep) are committed so `go:embed all:dist` always
# finds something at compile time.
internal/wsserver/dist/_expo/
internal/wsserver/dist/metadata.json
internal/wsserver/dist/assets/
internal/wsserver/dist/favicon.ico

# OS
.DS_Store
Thumbs.db
